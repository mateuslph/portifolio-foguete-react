name: Deploy para Hostinger via SSH

on:
  push:
    branches:
      - main  # ou o nome do branch principal

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout do código
      uses: actions/checkout@v3

    - name: Deploy via SSH
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: 77.37.127.114
        username: u885558733
        key: ${{ secrets.HOSTINGER_SSH_KEY }}
        port: 22
        script: |
          cd /home/seu_usuario/domains/umprogramax.com/public_html

          git pull origin main
